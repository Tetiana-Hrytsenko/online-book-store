databaseChangeLog:
  - changeSet:
      id: create-role-table
      author: tetiana-hrytsenko
      changes:
        - createTable:
            tableName: roles
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: name
                  type: enum('ROLE_USER', 'ROLE_ADMIN')
                  constraints:
                    nullable: false
                    unique: true
